// import { execSync } from 'child_process';
// require("./_set-configs")

// const [migrationName] = process.argv.slice(2);

// execSync(`DB_URL=${process.env.DB_URL} pnpm prisma migrate dev --name ${migrationName}`, { stdio: 'inherit' });