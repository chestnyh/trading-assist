# object-navigator

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build object-navigator` to build the library.

## Running unit tests

Run `nx test object-navigator` to execute the unit tests via [Jest](https://jestjs.io).
