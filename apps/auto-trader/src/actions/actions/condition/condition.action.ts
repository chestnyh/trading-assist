/**
 * TODO add description
 * @param args 
 * @param heap 
 * @param dependencies 
 * @returns 
 */
export default function condition (
    args: any, 
    {
        sequenceContext
    }): boolean {

    const {
        condition
    } = args;

    // TODO make this work with conditions

    return true;
}