# Development Tools

Development utilities for local and integration-test workflows.
This folder contains scripts and Nx targets used to check environment files, prepare volumes, start external dependencies, and run commands with environment-based configuration.

Use these tools to orchestrate API integration test setup and cleanup (pre-e2e, e2e, post-e2e) in a consistent, repeatable way across local environments.

